# chrome-tab 新标签页

个人浏览器新标签页:背景壁纸 + 时钟 + 搜索框 + 用户可自由编排的图标网格,图标分布在多个页面间。

技术栈:前端 React + Vite,后端 Hono/Node + SQLite 单文件(WAL),前后端共享契约类型(pnpm workspace,ADR-0018);部署为 Caddy + backend 两容器(ADR-0019,Java + MySQL 旧栈已下线)。

## Language

**图标 (Icon)**:
仪表盘上用户可操作的最小元素。每条网站快捷链接、每只自选股、整条更新日志流各自为一个图标;「分组」也是一种图标。图标默认占一格,类型可声明跨格占多格(见 ADR-0021),但无用户可调的尺寸档位(ADR-0016);可拖拽排序、可跨页面移动;网站链接图标还可被收进「分组」作为子图标(不计「页面容量」,移出后落回本页)。
_Avoid_: 磁贴、Tile、卡片——旧实现把"快速导航/行情/日志"整组称为"磁贴";新模型以单个元素为图标,「分组」只是收纳「网站链接」的一种图标类型,不再有磁贴式组容器。

**页面 (Page)**:
图标的容器,渲染为走马灯(Carousel)中的一屏,且是一个**固定画布**:内容必须在视口内完整呈现,不产生纵向滚动条。滚轮事件用于在页面之间切换,而非在页面内滚动(例外:跨格大 tile 的滚动主体内部优先消化滚轮、到边即停——不链式翻页;2026-08-24 随 ADR-0021 引入,08-25 因实测反直觉改为彻底消化,想翻页须把指针移出 tile)。页面是一等公民:用户可新增、改名、排序、删除。图标可跨页面拖拽。
_Avoid_: 屏、slide、标签页(tab)。

**图标名称 (Icon Label)**:
图标块下方外置的一行文字:网站链接图标显示站点名,「分组」显示组名。块是图标本体,名称独立在块外(见 ADR-0015 的 iOS 主屏分层)。样式(显隐、字号、颜色)是「布局设置」的一组,同管普通图标与分组。「上块下字」结构(块 + 名称行 + 块内主/次行字号档)的实现载体是前端 `Tile` 组件(components/Tile.tsx,组装与字号档的唯一来源)——「Tile」是实现名,不是领域概念(「图标」仍不叫磁贴)。
_Avoid_: 图标文字、文字标签。

**派生 favicon (Derived Favicon)**:
「网站链接」图标的默认图形:渲染时由图标 `data` 中的网址即时派生的第三方 favicon 服务 URL,不落库,网址变则图随变。是「图标覆盖」为空时的回落。
_Avoid_: 默认图标、原始 favicon。

**图标覆盖 (Icon Override)**:
「网站链接」的可选图形,用户在新增/编辑表单里从「站点信息」的图标候选中选定、手输图片地址或从本机上传图片;本机只指文件来源,保存后的图片随账号同步。渲染优先级:覆盖 > 派生 favicon——覆盖是显式手动意图,改网址不自动清除,编辑时可手动清空回落派生。
_Avoid_: 自定义图标、icon 字段(实现名)。

**站点信息 (Site Info)**:
新增/编辑「网站链接」时按网址向后端一次性抓取的表单辅助数据:页面标题(默认填入「图标名称」)与图标候选若干(解析页面图标声明的结果 + 派生 favicon)。后端代理抓取、带短 TTL 缓存、不持久化;表单之外无消费方,图标最终形态只由「派生 favicon」与「图标覆盖」决定。
_Avoid_: 网站信息、meta 抓取、网页预览。

**格子 (Cell)**:
页面画布的最小计量单位。固定 9×9 网格(9 列 × 9 行,2026-08-23 由 8×8 扩容为跨格大 tile 腾格,ADR-0021);格子的**数量**固定、不随图标数量变化,但网格的**像素几何**(宽度/横向间距/竖向间距)可由用户经「布局设置」调整。每个图标默认占一格,声明跨格的类型占其 w×h 格(ADR-0021)。
_Avoid_: 槽位、slot。

**页面容量 (Page Capacity)**:
一页可容纳的格子总数 = 固定 9 × 9 = 81 格,以格子数计、不受「布局设置」的像素几何影响。计量只算页面**顶层**图标,按各图标占格数求和(跨格类型按 w×h 计,ADR-0021):「分组」按其固定占 1 格计,组内子图标不计。当顶层图标占用格子之和达到容量时,该页拒绝再新增/拖入图标,提示用户新建页面或移至其它页。

**布局设置 (Layout Settings)**:
用户可调的仪表盘显示设置,分五组——**网格**(整体宽度、横向间距、竖向间距)、**背景**(雾化:页面主面板的底色浓度,即壁纸透出的清晰度;壁纸上的暗色可读性遮罩不开放)、**搜索栏**(宽度、显隐)、**时钟**(显隐、字号、12/24 小时制)、**图标名称**(显隐、字号、颜色)。网格组只改**像素几何**,与「格子」「页面容量」的 9×9=81 计数正交:不增减格子、不影响容量。图标整体大小(favicon 基准、块内边距、小组件字号)不开放调节,由代码固定——曾以「图标缩放」开放(ADR-0016),因调节引发图标展示不稳定而撤除(ADR-0033)。设置随账号持久化,跨设备共享。「默认搜索引擎」寄放同一面板,但是行为设置、概念上不属布局。
_Avoid_: 主题/theme(指明暗模式,另一项独立设置)、图标缩放(已撤除的旧设置,大小由代码固定,ADR-0033)、背景透明度(正名「雾化」,且不含壁纸遮罩)。

**布局草稿 (Layout Draft)**:
控制抽屉「布局」tab 打开期间的「布局设置」暂存态:控件改动先落草稿并乐观写缓存实时预览,松手(pointerup/keyup/change)或关闭抽屉(Esc/遮罩/×,兜底)时整份 PUT 为权威值;PUT 失败还原服务端值,不留幻影预览。干净草稿(未改动)随服务端并发更新 reseed;一旦改动即脏,关抽屉时本端 LWW 胜出(ADR-0006)。实现载体:前端 `lib/layoutDraft.ts` 纯协议 + `hooks/useLayoutDraft.ts` 接线。
_Avoid_: draft state、临时设置、未保存更改(用户视角无「保存」动作)。

**乐观 mutation (Optimistic Mutation)**:
乐观缓存写协议:取消在途读取 → 快照 → 乐观写目标态;失败还原快照,落定失效重拉服务端权威值——约束由服务端 409 把关,前端乐观失败即回滚。骨架单点于前端 `lib/optimisticMutation.ts`(纯回调工厂,按 query key 与缓存类型参数化,可测):config 写操作(删除/改配置/重排/移动/建组/解散,key = `CONFIG_KEY`)与「待办」点掉完成(key = `TODO_KEY`,缓存可为 null)共用,各写 hook 仅声明 mutationFn 与乐观态算法;「新闻」勾选与「已了解」标记的 onSuccess 权威写(响应即数据,先取消在途、无乐观写无还原无失效重拉,失败缓存不动)走同文件姊妹出口,消费 hook 亦仅声明 mutationFn。判空双层契约(ADR-0044):乐观写门 null 缓存跳过(updater 永不在空值上被调,类型以 `NonNullable<T>` 表达),还原门 null 快照照常还原。config 聚合的缓存 key 唯一声明于 `api/config.ts` 的 `CONFIG_KEY`,乐观写/快照还原/镜像落盘/拖拽会话新鲜度/缓存订阅嗅探全引用之。「布局草稿」的乐观写发生在松手前,不经此骨架;「拖拽编排」的连续乐观流(高频落点搬移 + 快照回滚)亦不经此骨架,见该词条——例外不承担骨架的取消前提:布局草稿在草稿会话首写前清一次在途读取;拖拽乐观流靠松手落定的失效重拉自愈,不以取消在途为前提。
_Avoid_: 手抄 onMutate/onError/onSettled 三件套(漏取消在途或漏还原即乐观态污染)、绕过骨架的缓存直写、散落的 ['config'] 字面量(key 引用常量)。

**拖拽编排 (Drag Protocol)**:
图标在网格与「分组」弹层间拖拽期间的乐观缓存流协议(06 同页排序、07 跨页、08 组弹层三役沉淀)。查看态与编辑态均可拖(组内排序两模式均可,移出分组仅编辑模式)。拖起即快照聚合缓存;拖拽中按落点种类(空页 / 图标 / 弹层容器 / 边缘翻页区)决策:跨页与组员移出**先乐观写缓存**(无网络,图标拖拽中即现身目标页),容量门前置预校验(对齐后端 requireCapacity,目标页满拒绝并提示);松手持久化——跨页按缓存最终态、同页按落点位序、组内重排按组内序列,统一走移动接口;Esc 整份回写快照,组员拖出后落空/落回弹层亦回写,不留幻影。合并手势(悬停达标且指针仍在目标)在松手时改走建组/入组,不走排序;分组弹层的关闭判定也在松手时:确已脱离组(落页面网格)才关,拖拽中途绝不卸载弹层。策略与协议双层单点(均可表驱动测试):落点决策(容量门、位序、合并手势的编辑模式门)于前端 `lib/iconDrag.ts`(决策函数 + over 事件解析 + ADR-0003 碰撞链);拖拽会话生命周期(快照取/整份回写、新鲜度——icons 一律取事件时刻缓存、会话开始守卫——缓存未就绪不开始)于前端 `lib/dragSession.ts` 纯状态机 + `hooks/useDragSession.ts` 接线(执行 Effect:写缓存/mutate/提示/合并手势计时与合格性判定),DashboardPage 只做事件绑定与幽灵/提示渲染。
_Avoid_: 拖拽逻辑(散在组件内的旧形态)、dnd handler(实现名)。

**详情 Modal 骨架 (Detail Modal Scaffold)**:
各扩展图标「详情 Modal」在 ModalShell(几何/Esc 栈/动画,ADR-0031)之上的公共结构层:标头(名称 + 可选刷新钮)、tab 条(当前 tab 所指实体被删后悬空,回落首个 tab)、主体查询状态机(加载中 / 失败可重试 / 空态 / 内容;不依赖数据查询的 tab——如「管理」——在数据失败时仍可达)与打开即对账(打开 Modal 即重拉最新;是否声明由各域对鲜度与上游压力取舍,非结构强制)。各域只声明 tab 派生、空态文案与内容主体;逐 tab 各持查询态的域(「AI 热点」三 tab)可只取状态机零件。实现载体:前端 components/DetailModal.tsx(复合 + 同文件状态机零件)与 lib/detailModalState.ts 纯决策函数(ADR-0040)。
_Avoid_: Modal 框架(泛 UI 概念)、ModalShell(下层壳,非本层)、弹窗模板。

**块内主体 (Tile Body)**:
跨格大 tile 标头之下的滚动榜区域,是「详情 Modal 骨架」在块内的对应物。公共骨架四处单点:滚动容器(原生滚动翻阅,触屏 pan-y 保原生滚动、TouchSensor 分流拖拽——ADR-0021 滚轮契约的实现载体)、行壳(圆角 pill;可点性四态——外链行/点击行/hover 行/纯静态行;不可点不做 hover 高亮,免暗示交互)、行数渲染窗(最近 30 行,看全量走「更多」Modal;「AI 热点」与「待办」声明全量翻阅)与 24h 红点窗(时间驱动、满窗自隐、无已读概念)。行内容(行内布局、字段、外链语义)归各图标类型;标头鲜度位不属于块内主体,按域各自计算(如「新闻」按源级最近成功抓取时刻)后经 BigTile 呈现。实现载体:前端 components/TileBody.tsx(容器 + 行壳 + 红点零件)与 lib/tileBody.ts(红点窗纯规则)。
_Avoid_: 块内列表、滚动区、榜单组件。

**悬浮宽限 (Hover Grace)**:
指针驱动的浮层显隐手势契约:鼠标进入触发元素即显示;离开后不立即收起——先给 250ms 宽限,到期做几何联合判定,指针仍在「触发盒∪浮层盒」外接矩形内(触发元素与浮层之间视觉间隙上的慢速穿越途中)则以 150ms 节奏重拍续期,真正离开才收;浮层自身被进入即取消待收。显示侧可带**首次交互门槛**:指针累计位移不足不显示,防刷新后静置指针恰好压在触发元素上幽灵触发。触发盒为离开时刻快照,浮层盒实时。实现载体:前端 `lib/hoverGrace.ts` 纯状态机(计时/几何/门槛,表驱动可测)+ `hooks/useHoverGrace.ts` 接线(ADR-0047);时钟 hover 弹层与「待办详情」快览卡是两个宿主,浮层挂载形态(子树 absolute / portal fixed)与域收口时机(数据变/切页/滚动即收)归宿主。
_Avoid_: hover-intent(实现行话)、防误触(只指门槛一面)、宽限计时器(实现细节非契约)。

**图标类型 (Icon Type)**:
类型身份、画格跨度与是否单例由前后端共享的单源元数据表(shared `iconTypes.ts`,ADR-0057——双端各自派生,消灭五份手写漂移)声明;标签、配置表单等前端专属元数据由前端元数据表(`lib/iconTypeRegistry.ts`,静态全覆盖,span/singleton 自单源表展开)持有(ADR-0001 契约)。图标块与详情的组件映射由静态全覆盖 UI adapter(前端 `components/iconTypeUi.tsx`)持有——有无详情由可选详情 renderer 表达,入口策略('block' 整块点击 / 'header' 标头「更多」,ADR-0022)随 adapter 声明(2026-08-28 起,原注册表 detail/detailEntry 字段退役)。类型分基础与扩展两类。
_Avoid_: 组件、widget、block。

**基础类型 / 扩展类型 (Base / Extension Type)**:
基础类型随产品内置、不可卸载(目前仅"网站链接")。扩展类型可插拔(目前为“股票”、“天气”、“更新日志”、“AI 热点”、“待办”、“视频更新”、“模型追踪”、“新闻”、“GitHub 趋势”、“服务器”、“倒计时”),是后续新增各类图标功能的扩展点。

**单例类型 (Singleton Type)**:
单源类型表(shared,ADR-0057)标记为单例的图标类型,全局仅允许一个实例;新增抽屉中已存在的单例类型会被禁用。目前为「AI 热点」与「待办」(榜单与待办三视图均全局唯一、无可绑实例参数)、「视频更新」(博主注册表是账号级后端数据)、「模型追踪」(全局模型档案非用户数据)、「新闻」(「新闻源」勾选是账号级后端数据)与「GitHub 趋势」(默认视图全局一份)与「服务器状态」(机器清单是 env 级部署配置)与「倒计时」(「重要日子」是账号级配置、「节假日」是内置枚举);「更新日志」曾是单例,多源化时移除标记(ADR-0020)。其余类型(网站链接、股票、天气、更新日志)可添加任意多个实例。

**详情 Modal (Detail Modal)**:
扩展类型图标的详情容器,承载图标块内放不下的完整视图(完整列表/预报/筛选/管理)。入口随图标形态:普通占格图标点块打开(如「天气」),3×2 跨格大 tile 走标头「更多」按钮(ADR-0022 范式)。内部为同构范式:查询态(空/加载/错误+重试)+ tab 导航(可含「管理」tab)+ 标头;各 tab 的内容由图标类型自持。区别于「待办详情」的二级对话框与「分组」的分组弹层(非模态)。实现载体:前端 `components/DetailModal.tsx` 骨架单点(标头/tab 条/查询状态机/悬空回落,ADR-0040)+ `lib/detailModalState.ts` 纯决策函数;十一家详情 Modal 均为声明式薄壳。
_Avoid_: 弹窗(泛称)、Drawer(2026-08-23 前的旧详情容器形态)。

**外源 (Upstream)**:
「更新日志」实例绑定的外部软件源,内置枚举(代码即配置,前后端共享):版本与发布日期默认取 npm 包,Codex 则整源走 GitHub Releases(版本、日期、正文一并,ADR-0050);若 npm 版本键与仓库日志不一致(Matt Skills),发布日期改取 GitHub Releases;IDEA 非 npm 发行,整源走 JetBrains Data Services releases API(版本、日期、正文一并,且仅统一发行版 IIU 一条活通道、不含 EAP);版本块原文来源三形态——仓库 CHANGELOG.md 地址(直取)、GitHub Releases 正文(合成,Codex:上游 CHANGELOG.md 是一行链接的存根,release 正文**预发布为空壳、正式版完整**,合成含预发布版本但仅正式版有条目,并剔除 commit 级 PR 清单等噪音小节)或 JetBrains Data Services 逐版摘要(合成,IDEA:whatsnew 摘要 HTML 转 bullet,首段发布模板句剔除,YouTrack 链接保留可点;无摘要的老版本仅列版本行)或博客「What's New」长文(合成,IDEA:blogFeedUrl RSS 只取大版本长文,追加为版本块尾 Blog post 小节——小版本公告与 whatsnew 逐条重复、What's fixed 为 issue 级清单,均不追加;RSS 深度仅最近十余条,**存在才展示**;RSS 失败吞错降级,增强臂不阻塞主链),均**可空**。三地址皆空者即「无原文源」,版本流取 npm 版本表并**剔除预发布版本**,详情只给版本与仓库外链、无译制。图标 `data` 存源标识;存量 `data=null` 的旧更新日志图标读侧兜底归默认源(Claude Code)。
_Avoid_: 数据源(泛指一切取数来源,含行情/天气)、渠道。

**更新日志 (Changelog)**:
扩展图标类型,非单例——每实例绑定一个「外源」,聚合该源的版本更新流(ADR-0020)。图标为 3×2 跨格大 tile(ADR-0022):标头显示源名与最新版鲜度,块内为版本滚动榜(一行一版本:版本号 + 相对发布时间,发布时间随源取 npm registry 或 GitHub Releases;滚动榜**剔除预发布版本**——块内是信号位,预发布发布频繁(如 Codex 日均 2-3 个)会近乎常亮红点,稀释「正式版更新了」的信号,ADR-0050;发布 24 小时内的版本行带红点新版本标记——时间驱动、满窗自隐、无已读概念);完整版本列表与更新详情走详情 Modal(按实例的源打开,**全量含预发布**——与块内信号位同源两态,全览位语义;预发布版本无正文则仅列版本行、无条目无译制;「最新」标记(accent/药丸)统一**稳定轴**——列表含预发布,但「最新」只认正式版,与块内同答案;入口 = tile 标头「更多」按钮,整块点击无操作,ADR-0022)。原文经后端 LLM 译制,每版终身只译一次、后台定时预取(ADR-0005、ADR-0017;取数 = 后端单 adapter 一次调用同源拿「原文 + 发布信息」,发布信息可空即部分成功显式化,ADR-0053;译制候选链/分段单点 translate.ts),未译旧版可在 Modal 按需补译并持久化——译文按版本块原文哈希入「译文表」,同原文块跨源共享;「无原文」外源(见「外源」)例外:无原文可译,Modal 仅列版本与外链。
_Avoid_: 日志磁贴、更新卡片(旧实现叫法)、底部 Drawer(2026-08-23 前的详情容器形态,ADR-0022 起为 Modal)。

**译文表 (Translation Table)**:
后端 LLM 译制的持久化产物:译文以**原文哈希**为身份存放,同原文终身只译一次、跨条目跨源共享——三个域各一张同构表(「更新日志」版本块、「新闻」标题、「GitHub 趋势」描述;ADR-0005/0029/0030)。哈希即身份:原文变化即新键自然重译,改回旧值免费命中;条目自身的生命周期(裁剪出窗、缓存过期)不牵连译文,孤儿译文不清理。译制失败或未配 Key 时保持原文(宁原文勿空)。语言判定口径随域而异:「新闻」按源清单认定英文源;「GitHub 趋势」全量送译、已是简体中文的由 prompt 约束原样回显(ADR-0036 修订,原逐条汉字启发式因整条跳过英中混排描述而撤)。存储机制(load/save 与批量补译编排)单点于后端 `translate.ts` 的哈希译文仓,域以**原文**为键消费、不感知哈希派生(ADR-0032/0034)。
_Avoid_: 翻译缓存(缓存暗示可淘汰,译文终身复用非缓存语义)、i18n(界面文案国际化,与外部内容译制无关)。

**译制阶段 (Translate Phase)**:
「更新日志」按需补译进行到哪一步的可见状态:哪个候选模型正在被调用、第几候选、已历时几秒。空闲状态而请求未返回 = 「排队中」——补译与后台刷新共用一条串行通道,前序任务未完前新请求等待、完成后自动开始(排队不是失败,区别于译制失败)。终态以请求回执为准,不属于阶段。
_Avoid_: 进度、进度条(补译单块为主,无总量可比)。

**分组 (Group)**:
一种特殊「图标」:`type='group'` 的图标行(见 ADR-0011),iOS 文件夹式的收纳容器:收起态为玻璃块内成员图标的 3×2 迷你预览(前 6 个),浏览成员走分组弹层(见 ADR-0015)。固定占 1 格、无嵌套;组名存于图标 `data`,默认「新建分组」,空名回落默认。成员只能是「网站链接」图标(子图标),组内不计「页面容量」,移出后落回本页。建组 / 加入分组 = 编辑模式拖图标悬停到另一图标或分组上合并(同一手势、同一放大反馈);解散 = 编辑模式分组上的 ×,子图标洒回本页;组内排序两种模式均可(分组弹层内直接拖,对齐页面网格「查看态直接拖」先例),成员编辑(✎ 改名称/网址/覆盖,同网格图标配置表单)与移出仍仅编辑模式(分组改名除外,走分组弹层点名称)。页面序列与组内序列各自独立按 `sort_order` 排列;组内超过 9 个时弹层按 9 个/页流式分页,分页只是展示切片、不是实体。
_Avoid_: 文件夹、folder(不叫文件夹)、标签组。

**编辑模式 (Edit Mode)**:
右键切换的编排态。在此模式下可编辑图标配置(data)、删除图标与管理页面;网格与组内拖拽排序两种模式均可(查看态直接拖,06 先例),「分组」的建组、加入、移出与解散仅在此模式(分组改名除外)。再次右键退出。
_Avoid_: 设置模式、customize mode。

**新增抽屉 (Add Drawer)**:
右上角"⚙"按钮唤起的「控制抽屉」中的"新增"tab(与"布局"tab 同抽屉切换),用于选择并配置待新增的图标类型。与编辑模式职责分离——新增图标不在编辑模式内完成。

**标的 (Instrument)**:
行情数据的指向对象:个股或指数(指数无「公司概述」,仅点位/涨跌)。规范代码为 symbol——两位市场小写前缀(sh/sz/hk/us)+代码,无分隔符(如 `sh600519`、`usAAPL`);美股代码不含交易所后缀。
_Avoid_: 证券(外延太宽,含债券)、股票(口语中混用于个股、指数与整个图标类型)。

**自选股 (Watchlist)**:
扩展图标类型,非单例——每个实例绑定一个「标的」,前端直连行情(报价/公司概述/估值/K线四路取数,不经后端代理,见 ADR-0004 的取向;script 注入生命周期的超时/清理/回调挂删单点于前端 `lib/scriptLoader.ts`,ADR-0046)。图标 `data` 存 symbol 与名称;新增或改绑标的经**标的检索**:按名称/代码/拼音关键词搜上游候选(腾讯 smartbox,script 注入通道,仅选择时用)选定,名称默认取候选带回的规范名、可改;输精确代码无候选时允许原样直提(OTC/粉单类搜不到,不因此堵死新增)。区别于「天气」(绑定位置、走后端代理)与「更新日志」(绑定「外源」)。
_Avoid_: 股票磁贴、股票卡片(旧实现叫法)、stock widget。

**公司概述 (Company Overview)**:
股票图标中,描述标的**公司**(而非指数)的相对稳定属性集合——主营业务、所属行业、地区、上市交易所、官网,以及市值/市盈率/市净率等随股价变动的派生量。区别于随行情秒级变动的「实时摘要」(价格/涨跌/成交)与历史价格序列「K线」。指数型标的(如上证指数、纳指)无公司概述,仅有点位/涨跌。
_Avoid_: 基本面、概述信息、公司信息、stock info、overview。

**分时 (Intraday)**:
自选股详情 K 线区的「当日」档:当日分钟级收盘序列,与历史日线「K线」同图异形——横轴为时刻而非日期,交易时段内随行情刷新,叠昨收基准线作涨跌锚点。非交易日呈现最近一个交易日的分时,由时间戳自证日期、无特判文案。
_Avoid_: 分时图(本产品只称「当日」档)、蜡烛图(K 线在本产品恒为收盘价折线)。

**时间档位 (Kline Range)**:
「自选股」详情 K 线区的档位胶囊:当日|近一月|近一年|全部(默认近一年)。每档声明自带全套语义——展示名、取数参数、是否「分时」、轮询节奏——单点声明、编译穷尽,取数(hook)与胶囊(Modal)只消费不判别,加档 = 加一个声明条目;「是不是分时」的谓词因此只有声明表一个真源。K 线图的档位语义(昨收虚线显隐、涨跌锚对昨收还是前一根、横轴日期还是时刻、y 域是否并入昨收)随声明分派,调用方无脑传昨收、不持判别。实现载体:前端 lib/kline.ts 的 KLINE_RANGES 与 klineChartModel。
_Avoid_: 档位判别散布(消费点各自推导「是不是分时」)、参数表与展示名两半分家(RANGE_PARAMS/RANGES 旧形态)。

**天气 (Weather)**:
扩展图标类型,非单例——每个实例绑定一个「位置」(城市),图标占 1×1 普通格(曾 3×1 跨格的小时序列形态,2026-09-01 收回单格):块内为**实况摘要**——状况图标 + 温度。存在灾害预警时块右上角显示**等级色警示点**:取 severity 最高的预警(CAP:Minor< Moderate< Severe< Extreme),颜色 = 预警等级色(后端下发的 color 直用,缺失按 severity 查标准色;蓝/黄/橙/红,与详情 Modal 预警色条同源),悬停可见预警名。城市名在下方名称行(多实例互区分的判据,取数失败也照常显示)。完整 24 小时预报、7 天预报、空气与灾害预警全文走详情 Modal(点块打开;无滚动主体,不入 3×2 大 tile 的「更多」标头范式)。图标 `data` 存该位置的城市名、行政区划(adm1/adm2)与经纬度(lat/lon);经纬度是五套数据(实况/24h/7d 预报/空气/预警)取数的统一坐标,城市搜索(和风 GeoAPI)只在选位置时用一次、解析成经纬度后存下。区别于「自选股」(绑定标的,前端直连行情)与「更新日志」(绑定「外源」,无位置);天气取数走后端代理(Key 不入前端,见 ADR-0009)。
_Avoid_: 天气磁贴/天气卡片(旧实现叫法)、weather widget。

**AI 热点 (AI Hot Topics)**:
扩展图标类型,「单例类型」——AIHOT 热点榜全局唯一、实例无可绑参数(区别于「天气」绑位置、「更新日志」绑「外源」),故全局仅一个实例。聚合 AIHOT 三路内容:热点榜(事件级聚合的排名流,每条含标题、报道源数与最新报道时间)、「模型精选」与「AI 日报」。图标占 3×2 跨格(ADR-0021):块内单列滚动的**「AI 日报」标题列表**(2026-08-25 前为热点榜,日报无排名不放序号,透出五分类标头作结构;标题单行截断,点条目外跳 AIHOT 阅读页),标头鲜度位显示按出刊时刻(每早八时)推定的相对时间;「更多」按钮开详情 Modal(与「天气」同范式),Modal 三 tab:「AI 日报」(默认,呼应块内)、「热点榜」(完整榜单,条目另给原文出处次链接)与「模型精选」;取数走后端匿名代理、短缓存对齐上游缓存节奏,数据是易失视图不落库——区别于「更新日志」的持久化译文。
_Avoid_: AI 资讯(名太泛,留给未来可能接入的条目流类型)、新闻磁贴、hot-topics(上游端点名,非领域概念)。

**模型精选 (Model Picks)**:
「AI 热点」详情 Modal 的第二 tab:AIHOT 上游「精选」流中「模型发布」分类(上游 slug ai-models)的透传视图。「精选」是上游的**条目级** LLM 策展(打分 + 精选理由逐条挑选),区别于热点榜的**事件级**多源印证聚合——同一 Modal 的两个 tab 正是这两种粒度的并置。窗口固定 7 天、至多 30 条;分类硬编码非用户可配(单例图标无实例参数)。条目主跳 AIHOT 站内阅读页(中文摘要 + 原文入口),原文出处作次链;摘要与打分不透传前端,深度阅读留在 AIHOT 站内。取数与「AI 热点」同范式:后端匿名代理、短缓存对齐上游 s-maxage,易失视图不落库。
_Avoid_: 模型榜(上游 /leaderboard 的模型排行榜,另一概念且无公开 API)、模型资讯、模型分类精选流(分类不是用户可选维度,只有「模型发布」一档)。

**AI 日报 (AIHOT Daily)**:
「AI 热点」3×2 tile 的块内主体与详情 Modal 的日报 tab(默认):AIHOT 上游每早八时(北京时间)定稿的**带日期编辑产物**——AI 筛选的今日重点,五分类编组(模型发布/更新、产品发布/更新、行业动态、论文研究、技巧与观点),条目 = 标题 + 摘要 + 阅读页/原文双链接(主跳站内阅读页,同「模型精选」范式)。块内只列标题(单行截断、悬浮全名,分类标头透出编组),摘要全显留在 Modal。区别于「AI 热点」的实时事件排名与「模型精选」的 7 天窗活流切片:**日报生成后当天不变,是快照不是流**(「模型发布/更新」分类与「模型精选」内容可能重叠,但一个是定稿存档、一个是活流)。只看最新一期;上游另有周报/月报与历史存档,不在本产品范围。取数与「AI 热点」同范式:后端匿名代理、短缓存,易失视图不落库。
_Avoid_: AI 资讯(名太泛,留给未来条目流类型)、每日精选/日报流(弱化「带日期定稿」语义)、daily(上游端点前缀,非领域概念)。

**模型厂家 (Model Provider)**:
对 AI 模型发布与官方资料承担一手责任的组织。首批跟踪智谱、OpenAI、Anthropic、xAI、月之暗面、DeepSeek 与阿里通义;千问(Qwen)、万相(Wan)是通义旗下的模型系列,Grok 是 xAI 的模型系列,都不是模型厂家。
_Avoid_: 模型公司、供应商、把 Grok 当厂家。

**跟踪厂家 (Tracked Provider)**:
已纳入模型发布跟踪、且已明确一手信源与专属发布判定的「模型厂家」。每家采用确定性专属解析与经一手来源核验的历史基线;新信源条目无法准确识别时只作待核验线索,不自动生成「模型动态」。它不是用户可自行增删的订阅项;新厂家只在其准确跟踪方式经确认后纳入。实现载体:一厂家一份 provider 定义(信源 URL + 解析器 + 匹配器 + 线索策略)单点于后端 `providers/` 目录,取数骨架(轮询/改版判定/入库/标陈旧)由 `runPoll` 统一持有,不随厂家复制(ADR-0038)。
_Avoid_: 订阅源、厂家配置、任意信源。

**模型追踪 (Model Tracking)**:
汇集全部「跟踪厂家」的 AI 模型与「模型动态」的「单例类型」扩展图标,范围不限于大语言模型。图标占 3×2 跨格:块内滚动展示全部「跟踪模型」,可用模型在前、已退役模型在后并标记;行展示模型名、厂家、模型种类、发布阶段、开放方式与最近动态,24 小时内的新动态带时间驱动红点、无已读概念;标头「更多」是详情唯一入口。详情 Modal 以「全部」+各模型厂家为 tab(随跟踪厂家扩容自动派生),末位另有固定「跑分榜」tab(「评测结果」按评测方原生单维度指数对档案内模型排序的只读视图,无种类过滤、不展开模型行);厂家 tab 下另挂一行「模型种类」过滤胶囊(与厂家 tab 正交 AND 组合、单选互斥,随「模型种类」枚举扩容自动派生),模型行在当前 Modal 内展开价格、上下文、限额、训练参数量、评测结果、信源与动态时间线,不套第二层 Modal;无搜索或手动排序。尽可能回填一手信源可证实的历史,已退役模型保留并标记。
_Avoid_: 大语言模型发布跟踪、模型发布(未选用的图标名)。

**模型种类 (Model Kind)**:
模型的唯一主要用途类型,与「发布阶段」和「开放方式」正交。枚为文本、多模态理解、图像生成、视频生成、音频/语音、向量、重排、审核/分类八类;推理、编程、视觉输入与工具调用是能力,不产生额外种类。
_Avoid_: 能力、阶段、模态、让同一模型重复归类。

**跟踪模型 (Tracked Model)**:
「模型厂家」独立命名且已实际推出的模型型号或变体。具有独立产品差异的变体(如 Sol/Terra/Luna、Opus/Sonnet/Haiku)各算一个;移动别名、`latest` 与日期快照只是引用方式,不另算模型。家族仅在固定快照形态在售时(如 Grok 4.20 的 `-0309` 系列),以该固定 ID 为本体入档。
_Avoid_: API ID(可能只是别名或快照)、模型家族(可包含多个独立变体)。

**模型档案 (Model Archive)**:
全局共享并持久保留的「跟踪模型」、「模型动态」与最新资料集合。用户只决定是否放置「模型追踪」图标,不各自复制档案;单个信源失败时保留其最后成功结果并标记陈旧。
_Avoid_: 用户模型库、实时抓取结果、一次性缓存。

**发布阶段 (Release Stage)**:
模型在厂家发布生命周期中的成熟度标记,如 Experimental、Preview、Beta、GA、Deprecated 与 Retired。阶段变化不改变模型种类或开放方式。
_Avoid_: 模型种类、开放状态、开放权重。

**开放方式 (Availability Mode)**:
模型对外可用的途径,如 API、开放权重或仅在厂家产品内可用;同一模型可同时具有多种方式。它是独立于「发布阶段」的标签维度。
_Avoid_: 发布阶段、渠道、部署方式。

**训练参数量 (Training Parameter Count)**:
模型厂家公告、官方论文、模型卡或官方权重配置明确披露的参数规模;未披露即为未知,不以媒体、社区或推算估值补空。MoE 模型分别记录总参数量与激活参数量。
_Avoid_: 估算参数、传闻参数、把 MoE 总参数与激活参数混为一值。

**评测结果 (Evaluation Result)**:
某个明确模型版本在某评测方的具体 Benchmark 上得到的分数记录,必须同时保留评测方、Benchmark、模型版本、分数、评测日期与原始链接。首版只接 Artificial Analysis,精确匹配不到模型版本则留空;AA 侧「已收录、与基线行同名、映射缺」的漏映射不再静默留空——落「待核验线索」提醒补映射(2026-09-01);分数定期刷新为最新快照,只有模型首次进入评测或 Benchmark 方法/版本变化才生成「模型动态」;评测方**首配接入**整轮静默不产动态——历史模型的真实首入日不可考,接入是系统事件而非模型事件。API Key 只由服务端环境提供;未配置时评测区显示「未配置」,不影响其余模型追踪。不将不同 Benchmark 的分数归一化或合成跨站综合分。按评测方**原生**单维度指数(如 Artificial Analysis 编程指数)对「跟踪模型」排序呈现的「跑分榜」属于已存分数的原样排序视图,不属于综合分或万能排名;榜单排序键只认评测方原生指数,不自行跨 Benchmark 合成。
_Avoid_: 综合分、万能排名、脱离模型版本或评测日期的分数。

**模型价格 (Model Price)**:
「模型厂家」官方公布的模型计费项,原样保留币种、计价单位、渠道、地区与生效日期。Token、图片、视频时长与语音时长不跨类型换算;Artificial Analysis 的价格属评测方数据,不覆盖厂家官方价格。
_Avoid_: 统一价格、最低价、脱离计价单位的数字。

**模型限额 (Model Limit)**:
「模型厂家」公开的模型技术上限或套餐限制,带单位、套餐、地区与生效日期等作用域。包括上下文/最大输出、媒体时长/大小、批量上限与公开速率限额,不读取个人账号的剩余额度。
_Avoid_: 个人配额、账号余额、不带作用域的限制值。

**模型动态 (Model Event)**:
与「跟踪模型」相关且可回链信源的变化记录,包括实际推出、API/首方产品/权重开放、发布阶段、价格、能力、限额、上下文、训练参数量、移动别名换指向与退役重定向、外部评测变化。别名换指向/退役重定向作为追加动态保留在所指模型与其原型号上,不改写原型号的既有发布历史。厂家事实以官方一手来源为准;外部评测保留评测方、基准、版本、日期与原始链接。Preview、Beta、Experimental 与 GA 均可纳入;纯预告、传闻与第三方网关上架不纳入。
_Avoid_: 模型发布事件(范围过窄)、更新(语义不明)、预告、网关上架、AIHubMix 上新。

**待核验线索 (Pending Clue)**:
「自动解析认不出但不应静默」的信号落库形态:三个来源——**整条未认领**条目(基线外更新块,含智谱托管第三方)、**部分认领残余 ID**(OpenAI changelog/百炼表格一行混排认领与基线外 ID,后者逐 ID 一条;`-latest` 引用别名不算)与 **AA 同名未映射**(AA 已收录、与某厂家基线行圆点归一同名、映射表缺,线索落该厂家名下)。三源同走一张 clues 表(键按来源分形态:整条=日期+ID 串、残余=裸 ID、AA=`aa:`+slug),30 天窗挡历史条目、读侧只取 7 天仍出现的;人工核验纳入基线/补上映射后条目不再产出、自然滚出,无需删行。线索是给人看的待办而非事实链:不自动生成「模型动态」,轮询首轮全量暴露存量缺口是价值不是噪音。
_Avoid_: 噪音过滤(前缀启发式口径已实测否决——7 家未映射达百条级、变体与旧模型淹没真信号)、忽略/确认状态(量级不支持状态机,按实例再加)、把「AA 收录了基线完全没有的新模型」当本信号(那由厂家信源的残余 ID 线索覆盖)。

**意外跳过 (Unexpected Skip)**:
解析器丢弃「实抓口径之外的形态」的可观察计数(ADR-0052):上游页面结构破坏型漂移(日期畸形、数据行错列、标题剥空)不再静默蒸发,每处落一条原始片段截断,轮询层非空即告警。与**结构排除**相对——后者是页面正常构成(表头行、非条目行、同 URL 重复卡、无日期宣传卡),每轮恒定出现,不计入;「意外跳过为空」因此是全网常态基线,非空即真信号,无需阈值或趋势记忆。空数组告警语义是「部分失败」信号:全量失败走既有「零条目判改版」通道。
_Avoid_: 把结构排除计入(常态噪音磨掉告警通道信誉)、为白名单外新类型词加启发式猜测(无猜判据缺失,结构性不可检测,ADR-0052 接受缺口)、落待核验线索表(跳过项无结构化归属与日期,构造不出线索,只走日志)。

**待办 (Todo)**:
扩展图标类型,「单例类型」——展示的是滴答清单账号级的待办视图,无可绑实例参数(区别于「天气」绑位置、「更新日志」绑「外源」),故全局仅一个实例。数据是**三视图**:**当天**(未完成且到期 ≤ 今晚,含过期)、**7 天**(未来 7 天 + 过期,含当天)、**收集箱**(滴答清单的无日期杂项清单,保留 id 字面量)。图标占 3×2 跨格(ADR-0021/0022,同「AI 热点」范式):主体 = 收集箱滚动列表(速记即入箱、入箱即见;悬浮条目即在条目右侧快览「待办详情」,点条目弹「待办详情」二级对话框深读),标头「更多」按钮是详情 Modal 唯一入口;详情 Modal 以 tab 切换三视图,点列表条目就地展开为左右分栏——左侧列表、右侧「待办详情」(窄窗降级为二级对话框)。它是首个**可写**的图标类型:此前所有类型均只读展示,待办可在 Modal 内点掉任务(即完成)与**速记**(一行文字速建任务,落收集箱,归期整理在滴答侧完成);编辑、删除、改期不进新标签页,归滴答官方客户端。取数与写回均走后端代理,凭据不入前端(同「天气」范式)。
_Avoid_: 滴答图标/滴答(「滴答清单」是上游外部系统,图标类型名是「待办」)、任务磁贴、todo list、TickTick(滴答清单国际版名)、今日待办(3×2 迭代前的旧口径,现为三视图之一)。

**待办详情 (Todo Detail)**:
单条待办的只读展示容器,三种形态:待办 Modal 内点列表条目就地展开的右侧分栏、图标块内点收集箱条目弹出的二级对话框(窄窗降级复用后者),或悬浮收集箱条目在条目右侧展开的快览卡(非模态,指针移开走「悬浮宽限」/列表滚动即收;键盘与触屏无 hover,走点条目路径)。编辑与完成仍在列表侧进行,详情不带写操作。
_Avoid_: 详情页、记录页。

**备注 (Note)**:
滴答待办自带的备注正文(上游字段名 content,非 note),markdown 文本;在「待办详情」中只读渲染,编辑归滴答官方客户端。可为空,空备注同样可开详情。
_Avoid_: 记录(暗示追加式日志)、描述。

**视频更新 (Video Updates)**:
扩展图标类型,「单例类型」——博主注册表是账号级后端数据、实例无可绑参数,全局仅一个实例。图标占 3×2 跨格:块内为**全分类混合**的单列滚动视频流(24h 内发布的条目带红点——按发布时间判、满窗自隐、无已读概念;行 = `博主名 · 相对时间` + 标题截断 + 平台标记,点行外跳原平台),标头「更多」按钮是详情唯一入口(ADR-0022 范式)。详情 Modal tab:全部(默认,混合时间流)→ 未分类 → 各分类 → 管理(博主添加/改分类/删除、分类增删改排序)。区别于「AI 热点」的易失代理:数据由后端定时轮询预取、博主与视频持久化落库(ADR-0023),前端只读;每博主滚动保留最近 50 条,入库即快照不回删(骨架单点于后端 pollPersist,ADR-0039)。
_Avoid_: 视频磁贴、订阅(未管理订阅列表)、追更。

**博主 (Blogger)**:
「视频更新」跟踪的视频发布者,YouTube 频道与 B站 UP 主的统一称谓,单归属恰一个「分类」(无分类即「未分类」桶)。添加 = 详情 Modal「管理」tab 粘贴主页链接,后端解析(同「站点信息」抓取范式);注册表与视频流是账号级后端数据(独立表,不塞图标 `data`,对齐「待办」范式)。连续 24 轮(1 天)取数失败标「取数失败」,不自动删除;取数路线见 ADR-0024。
_Avoid_: UP主、频道(平台各自的叫法)、作者。

**分类 (Category)**:
「视频更新」中博主的单归属组织维度,用户自由创建(仅起名、可排序);「未分类」是默认桶(category 为空的博主归此、不可删改),不是实体。分类是详情 Modal 的 tab 维度,不是画布维度——块内视频流始终全分类混合。
_Avoid_: 分组(那是画布上收纳「网站链接」的图标类型)、文件夹、标签。

**新闻 (News)**:
扩展图标类型,「单例类型」——「新闻源」勾选是账号级后端数据、实例无可绑参数,故全局仅一个实例。图标占 3×2 跨格:块内为**全源混合**的单列滚动新闻流(24h 内发布的条目带时间驱动红点——按发布时间判、满窗自隐、无已读概念;热榜类源无逐条发布时间,行内时间与红点对无时间条目自然省缺,标头鲜度位以**源级**最近成功抓取时间计;行 = 源名 · 相对时间 + 标题截断,点行外跳原文),标头「更多」按钮是详情唯一入口(ADR-0022 范式)。详情 Modal tab:全部(默认,混合时间流)→ 各「新闻源」 → 管理(勾选源)。数据由后端定时轮询预取落库(对齐「视频更新」范式,ADR-0023),每源滚动保留最近 50 条,入库即快照不回删(轮询串行与「同事务先插后裁」入库骨架单点于后端 pollPersist,ADR-0039);抓取函数移植自开源 newsnow(MIT)的源定义。英文源(Hacker News、Product Hunt)的标题经后端 LLM 译制为中文展示(ADR-0029):译文按标题原文哈希入「译文表」终身复用、跨源共享;补译内联在取数成功之后——取数失败连累译制静默跳过,故「英文没译」的常见根因是取数/代理故障而非译制本身(诊断见 `docs/agents/live-diagnosis.md`);译制中、失败或未配 Key 时保持英文原文(宁英勿空),行内悬停可见英文原文以供核对;译制保留 HN 惯用前缀(Show HN: 等)与方括号标注([video] 等)原样。区别于「AI 热点」的匿名代理易失视图——新闻落库持久化。
_Avoid_: 新闻磁贴、资讯(名太泛,同「AI 热点」词条的 avoid)、RSS 订阅(泛订阅器产品形态,已否决)。

**新闻源 (News Source)**:
「新闻」的内置枚举源清单,代码即配置(前后端共享,同「外源」模式):每个源 = 移植自 newsnow 的一段抓取函数 + 中文名等展示元数据。用户只能在详情 Modal「管理」tab 勾选启用/停用(账号级数据,勾选了才轮询该源),不能添加自定义源——区别于「博主」(粘贴链接添加)与「外源」(无勾选,实例即绑定)。
_Avoid_: 数据源(泛指一切取数来源,含行情/天气)、订阅源。

**GitHub 趋势 (GitHub Trending)**:
扩展图标类型,「单例类型」——默认视图全局一份、实例无可绑参数。图标占 3×2 跨格:块内为**今日趋势**单列滚动榜(行 = 语言色点 + repo 名 + 周期内 star 增量,点行外跳仓库页,悬停 title 给 repo 名与描述;标头名直达 github.com/trending,鲜度 = 缓存抓取时刻),标头「更多」按钮是详情唯一入口(ADR-0022 范式)。详情 Modal:口语语言 / 编程语言 / 周期**三行筛选胶囊**正交组合(精选子集——上游全量为几百项不铺开;不持久化,每次打开回默认今日视图),行 = repo 名 + 总 star / 描述(独立成行、完整换行**永不省略**——用户了解项目的第一途径)/ 语言色点·语言名 + 周期内增量,点行新开 tab;行尾挂「已了解」勾钮(块内同步渲染其背景,见「已了解」词条)。描述经后端 LLM 译制为中文展示(全量送译,已是简体中文的 prompt 约束原样回显;译文按描述哈希入「译文表」终身复用,ADR-0030/0036):译制状态两段呈现:补译新鲜窗内保持原文并随「译文生成中」微标(前端 15s 短轮询接力到译文);超窗仍有缺口即「N 条描述暂未译出」聚合提示条 + 重试翻译钮(POST retry-translation 显式补一轮,fire-and-forget 由轮询收果),悬停可见原文核对。数据:GitHub 无官方 API,靠 trending 页 HTML 解析;筛选组合是笛卡尔积,只保热默认组合,其余组合按需现抓 + 后端内存缓存,**榜单不落库**(ADR-0028;描述译文是唯一例外,见「译文表」)——区别于「新闻」的落库持久化。曾为「新闻源」之一,独立成图标后从源清单移除。
_Avoid_: 趋势磁贴、热榜(泛称,与「AI 热点」语境混淆)、trending(上游端点名,非领域概念)。

**已了解 (Known Mark)**:
「GitHub 趋势」的用户持久勾标记,语义是「我已了解这个项目」——**项目级**认知,以 repo 名(owner/name)为身份,跨日期与语言/周期筛选一致携带,在块内「今日趋势」榜与详情 Modal 两处同渲染(行底淡绿透明背景 + 勾标),已了解行保持趋势原序不改排名。永续直到手动取消,无过期、无批量清空;标记手势只在详情 Modal 行尾勾钮(块内行是外链,不设手势)。身份随 GitHub 侧 owner/name 现值:仓库改名/转手后旧标记不随行,成孤儿视同新项目(不清理,已知限制)。区别于红点:红点是时间驱动、满窗自隐、**无已读概念**的条目级信号;已了解是用户驱动、项目级的持久状态,不随榜单轮换失效。
_Avoid_: 已读(红点系统明言无已读概念,且那是条目级动作)、收藏(无收藏夹语义)、屏蔽/忽略(榜单照常展示,仅视觉区分)。

**服务器状态 (Server Status)**:
扩展图标类型,「单例类型」——监控对象清单是部署级 env 配置,实例无可绑参数。图标占 3×2 跨格:块内每台机器一行**简单信息**(状态点 + 机器名 + CPU/内存百分比;离线红点 + 旧数据时间标注),标头「更多」按钮是详情唯一入口(ADR-0022 范式),鲜度 = 最新快照时刻。详情 Modal:tab 按机器分页,页内 = 概览数字块(在线时长/CPU/负载/内存/磁盘/失败单元)+ CPU·内存使用率 24h sparkline + 服务(systemd,timer 附上次触发结果)与容器(docker)状态清单两列。数据:thinkpad/aliyun 两台物理机各跑一个 servermon exporter(Python stdlib,~10MB,源码在 thinkpad-ubuntu 仓库);backend 按需抓取 60s TTL 快照(**可达性由取数成败兼任**,无独立拨测组件;失败降级宁旧勿空,保留最后成功快照并如实标注陈旧)+ 每 10 分钟采样数值曲线落库(server_samples 全局表,无保留策略;服务/容器状态只走快照不落库)。两条取数链路均经容器 host-gateway(容器内 127.0.0.1 是容器自身,frp remotePort 与 exporter 都监听在宿主命名空间):thinkpad 走宿主 frp remotePort 10001(公网面 token 校验),aliyun 走宿主 7800(安全组不放行,公网不可达)。加监控的 systemd 服务 = 机器上 units 清单文件加一行重启 exporter,不发版。
_Avoid_: 服务器监控(泛称)、拨测/心跳(本域无独立组件,可达性是取数副产品)、servermon(exporter 实现名,非领域概念)。

**农历宜忌 (Lunar Almanac)**:
时钟的组成部分(无独立开关,随时钟显隐):日期行在公历后附农历(干支年+月日,如「丙午年七月十二」,逢节气以括号附注),第三行展示「宜/忌」各 3 条——按当代常用词白名单优选、保持典籍原序,古语词沉底;完整列表由悬停 `title` 承载。数据为《钦定协纪辨方书》整理表的**前端本地推算**(lunar-typescript 内置,与时钟同为确定性本地计算、不经后端、按天重算)。只展示当天,不提供任意日期查询。
_Avoid_: 黄历/老黄历(民间载体名)、择吉(指背后的术数体系,非本产品概念)、节气(农历的附注部分,不单列概念)。

**倒计时 (Countdown)**:
扩展图标类型,「单例类型」——数据是账号级配置+内置枚举、实例无可绑参数,故全局仅一个实例。图标占 1 格(普通占格图标,整块点击打开详情,同「自选股」入口策略):块内常显**下一条**临近条目——两路数据源(内置「节假日」+用户「重要日子」)混排取最近一条,**不限 30 天窗**(窗口是弹层列表语境的取舍,常驻图标的意义是看着它逼近),主行 = 剩余天数(当天「今天」、次日「明天」、其余「N 天」)、次行 = 条目名称,下方图标名称行固定「倒计时」。块内文字走**提醒阶梯**(2026-09-02):剩余 ≤1 天主/次行转琥珀(注意语义,不撞 accent/danger/涨跌立法)、当天主行叠加粗 + 光晕呼吸(countdown-glow,reduce 下仅静态光晕)——恒定高亮会稀释提醒力,高亮只在逼近时出现。详情 Modal **日历化**(2026-09-03,ADR-0054,原双 tab 撤):打开即当月月历(3xl 大画布,周一开头,月份可前后翻,当月视图只标当月内的天),可切**年视图**(4×3 月份壁,迷你点阵带日期数字,本月 accent 锚点,点卡钻取进月;色语与月历同源),「今天」钮一键归位。格内副行 = 节日名(内置清单,含不放假的文化节日)> 节气 > 农历日(「二十/廿一」形态;R6iii「不做农历」同日被用户参考图推翻,ADR-0054 修订);休/班为右上角标(法定安排,经「节假日」词条的上游链路);重要日子琥珀底(用户条目,年度条目取当年换算日,点格子即开编辑表单——「重要日子」的编辑入口全局唯一在此);「+ 添加」保留;不做列表视图与点空格新建。时钟 hover 弹层另保留**只读**的倒计时分区:30 天窗口内两路混排按剩余天数升序,窗口内无数据时分区整体隐藏(编辑入口迁图标后无保留理由)。双形态与块内不限窗的取舍见 ADR-0043。年度条目过期后滚次年再进窗;一次性「重要日子」过期次日即消失。重要日子为账号配置;节日名仍为前端本地推算,法定休/班走后端上游(ADR-0054)。日历格语义——三源合并(同日撞期共存)与逐格决策(底色四层链/副行优先级/休班角标/可点性)——单点于前端 `lib/countdown.ts` 纯函数(buildCellMarks/cellModel,ADR-0056),月历大格与年壁迷你点同 interface 消费,组件只做渲染映射。
_Avoid_: 纪念日倒计时(「纪念日」只是其中一类条目)、任意日期查询/日历工具(月视图是倒计时数据的呈现,2026-09-03 起推翻旧「无日历」裁决,ADR-0054;任意日期跳转仍无)、提醒(无通知动作)。

**节假日 (Holidays)**:
「倒计时」的数据源,**双轨**(2026-09-03,ADR-0054):①**内置节日清单**——中外节日固定清单(中国法定+传统、国际/西方固定与浮动节日),代码即配置,用户不可增删改,每年自动推算(农历节日经 lunar-typescript 换算为当年公历,浮动节日按规则计算,除夕随腊月大小浮动);承担倒计时条目混排与日历格的节日名小字。②**法定休/班标记**——含调休的官方放假安排(2026-09-03 推翻本词条旧「不跟踪」裁决),经 ics 订阅上游(ical.muhan.org rest.ics/work.ics,单日 VEVENT)由后端拉取入内存快照(TTL 24h,独立端点供日历消费;上游挂或次年安排未公布时降级为无休/班标,不报错),国内直连进 NO_PROXY。区别于「重要日子」(用户配置)。
_Avoid_: 节日配置(内置清单与休/班数据用户均不可增删)、万年历/历史假日查询(上游数据只服务日历标记)。

**重要日子 (Important Dates)**:
「倒计时」的用户配置数据源:每条 = 名称 + 日期(公历或农历;农历生日/纪念日按当年对应公历日推算,已过则取次年)+ 重复方式(每年循环,或仅一次)。寄放于「布局设置」(同「默认搜索引擎」寄放先例,ADR-0026 不另立账号级表),随 config blob 走同步/镜像/备份/和解;编辑入口在「倒计时」图标详情 Modal(全局唯一),不在控制抽屉。
_Avoid_: 重要日志(笔误)、纪念日(仅指年度纪念类条目,不是集合名)、日程/事件(无时间点、无提醒)。

**重定向扩展 (Redirect Extension)**:
接管浏览器新标签页的 Chrome 扩展,唯一职责是把新标签页导航到「目标页 URL」。它是产品(独立部署的 Web SPA)的**入口基础设施**,不是页面的宿主——不打包任何产品代码,页面的 origin、存储与部署形态均不受它影响。
_Avoid_: 新标签页插件、宿主(见 ADR-0010,扩展不是宿主)。

**目标页 URL (Target Page URL)**:
新标签页最终打开的网页地址,由用户在扩展提供的配置入口修改,出厂默认指向本地开发服务器;本地开发与远程部署只是同一配置的两个取值,扩展不感知环境概念。
_Avoid_: 指定网页、首页(浏览器 homepage 是另一回事)。

**会话 (Session)**:
一次登录建立的设备级凭据,载体是 HttpOnly 持久 cookie 指向服务端会话表的一行。寿命是 **30 天滑动窗口**:活跃即永生(每次使用自动续满),连续闲置 30 天才需重新登录;续期按日节流,每设备每天最多一次写。服务器重启不掉线(会话落库);登出 = 删本设备会话;改密码 = 手工流程中顺带清空全部会话(所有设备重登)。
_Avoid_: 记住我、token、JWT。

**请求体校验 (Request Body Validation)**:
后端写端点请求体字段校验的小件族契约:坏 body 收敛 null、字段级 400(必填/缺省落默认/可空三种整数形态;名字按 @NotBlank @Size(max=NAME_MAX) 语义 trim 后落库)、嵌套定位前缀(「本地镜像」全量替换 blob 的 `icons[0].type`)。「图标」条目校验同表两入口合一:icons 六端点与 config PUT 走同一批件(type 枚举 + data/WebP 校验 `reqIconType`/`reqDataField` 域绑定 icons.ts),全量替换不再持旁门校验。实现载体:无域小件族(`jsonBody`/`reqName`/`reqInt`/`optInt`/`optNullableInt` + `NAME_MAX`)单点 backend `common.ts`(ADR-0048);`c.req.json()` 全仓唯 jsonBody 触达(grep 契约断言把关)。刻意分家:页面重排的 sortOrder 显式 null 必 400;blob 页名不 trim(忠实恢复,trim 是页面端点写入语义)。
_Avoid_: 校验器/validator(无 schema DSL,一组纯函数)、validation 库。

**从未取到 (Never Fetched)**:
某数据源在后端**从未成功从上游取得过任何一版数据**的数据态,与「取到过但为空」(走空态文案)、「网络失败」三分;上游失败时后端宁旧勿空(沿用上次成功数据),故该态出现必为从未成功。对用户呈现为刷新失败——与网络失败同文案、可重试,自愈靠轮询(失败态下轮询不断);曾有数据后转入该态时,详情视图显示失败而块内沿用最近成功数据(宁旧勿空对齐,不闪空)。wire 形状 HTTP 200 + null body;三态是后端立法的域语义(cachedOrNull 原语,ADR-0042;后端注释称「从未成功」,同义),前端在取数 seam 归一为失败:顶层 `T|null` 协议的 queryFn 经 `fetchNonNull` 声明「null 即失败」,消费端只面对加载/失败二态;该态不参与 retry(分钟级持续失败,重试无翻盘收益),网络错照旧重试一次(实现载体:前端 `api/client.ts` 的 `fetchNonNull` + `retryUnlessNeverFetched`,ADR-0049)。区别于「天气」的桶内 null(分桶部分失败,随批量响应部分成功,不适用整体归一)。
_Avoid_: 无数据/空数据(那是「取到过但为空」,走空态)、缓存未命中(后端内部机制词,非用户可感数据态)、null 态(实现形状非域概念)。

### 数据同步与备份

**本地镜像 (Local Mirror)**:
浏览器 IndexedDB 里按用户存的一份完整配置副本(`{pages, icons, layoutSettings}` + `updatedAt` + `dirty`,其中 `layoutSettings` 即「布局设置」),随在线写入实时更新,离线时作临时权威。按 userId 分键,登出不清空(作恢复来源)。主题(theme)为永久深色、不持久化、不入镜像。
_Avoid_: 本地缓存(指 React Query 的内存视图,刷新即失)。

**服务端权威 (Server Authority)**:
在线时服务端是配置的唯一事实来源,写入先到服务端再回写本地镜像;仅服务端不可达时,本地镜像临时接管。
_Avoid_: 主库、master。

**离线待推送 / 脏 (Offline Pending / Dirty)**:
服务端不可达期间只改本地镜像并置 `dirty`,尚未上服务端;重连后经「和解」决定是否推送。
_Avoid_: 未同步、pending changes。

**和解 (Reconciliation)**:
在加载、写入成功后、以及从离线恢复在线时,比较本地"上次服务端确认时间"与服务端 `config_version.updated_at`,新者整份赢(整体-blob LWW)。这是双向恢复的判据。
_Avoid_: 同步(太宽泛)、合并(指导入时的语义合并,另一回事)。

**备份 (Backup) / 恢复 (Recovery)**:
备份 = 导出含 `schemaVersion` 的完整 JSON;恢复 = 从备份或存活的另一端把数据还原到丢失端。导入导出是手动的备份/恢复通道,双端镜像是自动的互保通道,两者目的相同(防丢失)、机制不同。
_Avoid_: 导入导出(是实现动作,非概念)。
