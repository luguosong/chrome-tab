# chrome-tab 新标签页

个人浏览器新标签页:背景壁纸 + 时钟 + 搜索框 + 用户可自由编排的图标网格,图标分布在多个页面间。

## Language

**图标 (Icon)**:
仪表盘上用户可操作的最小元素。每条网站快捷链接、每只自选股、整条更新日志流各自为一个图标。图标具有类型与尺寸,可拖拽排序、可跨页面移动。
_Avoid_: 磁贴、Tile、卡片——旧实现把"快速导航/行情/日志"整组称为"磁贴";新模型以单个元素为图标,组容器不再存在。

**页面 (Page)**:
图标的容器,渲染为走马灯(Carousel)中的一屏,且是一个**固定画布**:内容必须在视口内完整呈现,不产生纵向滚动条。滚轮事件用于在页面之间切换,而非在页面内滚动。页面是一等公民:用户可新增、改名、排序、删除。图标可跨页面拖拽。
_Avoid_: 屏、slide、标签页(tab)。

**尺寸 (Size)**:
图标的三档大小:小 / 中 / 大,在 8 列网格中分别占 1×1 / 2×2 / 3×2 个格子。尺寸同时决定网格跨度与展示的信息密度(小=图标+名称,中=图标+名称,大=图标+名称+实时摘要;富数据类型(如股票)有按尺寸分档的专属信息布局,不沿用此通用密度);小与中的差别在跨度(1×1 vs 2×2)与 favicon 尺寸,而非是否显示名称。每个图标可单独切换尺寸。
_Avoid_: 规格、size class。

**格子 (Cell)**:
页面画布的最小计量单位。固定 8×8 网格(8 列 × 8 行);格子的**数量**固定、不随图标数量变化,但网格的**像素几何**(宽度/间距/缩放)可由用户经「布局设置」调整。每个图标按其尺寸占据若干格子。一格 = 一个小图标的占地。
_Avoid_: 槽位、slot。

**页面容量 (Page Capacity)**:
一页可容纳的格子总数 = 固定 8 × 8 = 64 格,以格子数计、不受「布局设置」的像素几何影响。当图标占用格子之和达到容量时,该页拒绝再新增/拖入图标,提示用户新建页面或移至其它页。

**布局设置 (Layout Settings)**:
用户可调的图标网格显示几何,含三项——**整体宽度**(网格在面板内的最大宽度、居中)、**图标间距**(格子之间的 gap)、**图标缩放**(各「尺寸」档位 favicon 像素与内边距的同比系数,不改占格数)。三项只改**像素几何**,与「格子」「页面容量」的 8×8=64 计数正交:不增减格子、不影响容量。设置随账号持久化,跨设备共享。
_Avoid_: 主题/theme(指明暗模式,另一项独立设置)、页面缩放/page zoom(此处缩放仅作用于图标本身)。

**图标类型 (Icon Type)**:
类型注册表中的一个条目,声明该类图标:如何取摘要、详情容器形态、默认尺寸与支持的尺寸档位、是否单例、以及配置表单。类型分基础与扩展两类。
_Avoid_: 组件、widget、block。

**基础类型 / 扩展类型 (Base / Extension Type)**:
基础类型随产品内置、不可卸载(目前仅"网站链接")。扩展类型可插拔(目前为"股票"、"更新日志"),是后续新增各类图标功能的扩展点。

**单例类型 (Singleton Type)**:
注册表中标记为单例的图标类型,全局仅允许一个实例(目前为"更新日志")。新增抽屉中已存在的单例类型会被禁用。非单例类型(网站链接、股票)可添加任意多个实例。

**编辑模式 (Edit Mode)**:
右键切换的编排态。在此模式下可拖拽排序、切换尺寸、编辑图标配置(data)、删除图标与管理页面;再次右键退出。
_Avoid_: 设置模式、customize mode。

**新增抽屉 (Add Drawer)**:
由右上角"+"按钮唤起的侧抽屉,用于选择并配置待新增的图标类型。与编辑模式职责分离——新增图标不在编辑模式内完成。

**公司概述 (Company Overview)**:
股票图标中,描述标的**公司**(而非指数)的相对稳定属性集合——主营业务、所属行业、地区、上市交易所、官网,以及市值/市盈率/市净率等随股价变动的派生量。区别于随行情秒级变动的「实时摘要」(价格/涨跌/成交)与历史价格序列「K线」。指数型标的(如上证指数、纳指)无公司概述,仅有点位/涨跌。
_Avoid_: 基本面、概述信息、公司信息、stock info、overview。

**天气 (Weather)**:
扩展图标类型,非单例——每个实例绑定一个「位置」(城市),展示该位置的当前天气、空气质量与灾害预警。图标 `data` 存该位置的城市名、行政区划(adm1/adm2)与经纬度(lat/lon);经纬度是三套数据(实况/空气/预警)取数的统一坐标,城市搜索(和风 GeoAPI)只在选位置时用一次、解析成经纬度后存下。区别于「自选股」(绑定标的,前端直连行情)与「更新日志」(单例、无位置);天气取数走后端代理(Key 不入前端,见 ADR-0009)。
_Avoid_: 天气磁贴/天气卡片(旧实现叫法)、weather widget。

### 数据同步与备份

**本地镜像 (Local Mirror)**:
浏览器 IndexedDB 里按用户存的一份完整配置副本(`{pages, icons, layoutSettings}` + `updatedAt` + `dirty`,其中 `layoutSettings` 即「布局设置」),随在线写入实时更新,离线时作临时权威。按 userId 分键,登出不清空(作恢复来源)。主题(theme)为永久深色、不持久化、不入镜像。
_Avoid_: 本地缓存(指 React Query 的内存视图,刷新即失)。

**服务端权威 (Server Authority)**:
在线时服务端是配置的唯一事实来源,写入先到服务端再回写本地镜像;仅服务端不可达时,本地镜像临时接管。
_Avoid_: 主库、master。

**离线待推送 / 脏 (Offline Pending / Dirty)**:
服务端不可达期间只改本地镜像并置 `dirty`,尚未上服务端;重连后经「和解」决定是否推送。
_Avoid_: 未同步、pending changes。

**和解 (Reconciliation)**:
在加载、写入成功后、以及从离线恢复在线时,比较本地"上次服务端确认时间"与服务端 `config_version.updated_at`,新者整份赢(整体-blob LWW)。这是双向恢复的判据。
_Avoid_: 同步(太宽泛)、合并(指导入时的语义合并,另一回事)。

**备份 (Backup) / 恢复 (Recovery)**:
备份 = 导出含 `schemaVersion` 的完整 JSON;恢复 = 从备份或存活的另一端把数据还原到丢失端。导入导出是手动的备份/恢复通道,双端镜像是自动的互保通道,两者目的相同(防丢失)、机制不同。
_Avoid_: 导入导出(是实现动作,非概念)。
