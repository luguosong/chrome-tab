# chrome-tab 新标签页

个人浏览器新标签页:背景壁纸 + 时钟 + 搜索框 + 用户可自由编排的图标网格,图标分布在多个页面间。

## Language

**图标 (Icon)**:
仪表盘上用户可操作的最小元素。每条网站快捷链接、每只自选股、整条更新日志流各自为一个图标;「分组」也是一种图标。图标具有类型与尺寸,可拖拽排序、可跨页面移动;网站链接图标还可被收进「分组」作为子图标(不计「页面容量」,移出后按原尺寸落回)。
_Avoid_: 磁贴、Tile、卡片——旧实现把"快速导航/行情/日志"整组称为"磁贴";新模型以单个元素为图标,「分组」只是收纳「网站链接」的一种图标类型,不再有磁贴式组容器。

**页面 (Page)**:
图标的容器,渲染为走马灯(Carousel)中的一屏,且是一个**固定画布**:内容必须在视口内完整呈现,不产生纵向滚动条。滚轮事件用于在页面之间切换,而非在页面内滚动。页面是一等公民:用户可新增、改名、排序、删除。图标可跨页面拖拽。
_Avoid_: 屏、slide、标签页(tab)。

**尺寸 (Size)**:
图标的三档大小:小 / 中 / 大,在 8 列网格中分别占 1×1 / 2×2 / 3×2 个格子。所有图标统一玻璃块外壳,块缘铺满画格 = 与网格线严格对齐(见 ADR-0015);通用类型(网站链接)= 块内 favicon 居中 + 名称外置,favicon 边长自跨度**自相似推导**——中 = 两个小图标紧拼、大 = 三个紧拼(含间距),推导值作块内上限(见 ADR-0014);富数据类型(股票/天气/更新日志)为小组件式排版,按尺寸分档信息密度(见 ADR-0007)。每个图标可单独切换尺寸。
_Avoid_: 规格、size class。

**格子 (Cell)**:
页面画布的最小计量单位。固定 8×8 网格(8 列 × 8 行);格子的**数量**固定、不随图标数量变化,但网格的**像素几何**(宽度/间距/缩放)可由用户经「布局设置」调整。每个图标按其尺寸占据若干格子。一格 = 一个小图标的占地。
_Avoid_: 槽位、slot。

**页面容量 (Page Capacity)**:
一页可容纳的格子总数 = 固定 8 × 8 = 64 格,以格子数计、不受「布局设置」的像素几何影响。计量只算页面**顶层**图标:「分组」按其固定占 1 格计,组内子图标不计。当顶层图标占用格子之和达到容量时,该页拒绝再新增/拖入图标,提示用户新建页面或移至其它页。

**布局设置 (Layout Settings)**:
用户可调的图标网格显示几何,含三项——**整体宽度**(网格在面板内的最大宽度、居中)、**图标间距**(格子之间的 gap)、**图标缩放**(favicon 边长与块内边距的同比系数,不改占格数、不改块尺寸——块缘即格线,见 ADR-0015;favicon 边长还随「图标间距」参与自相似推导,见 ADR-0014)。三项只改**像素几何**,与「格子」「页面容量」的 8×8=64 计数正交:不增减格子、不影响容量。设置随账号持久化,跨设备共享。
_Avoid_: 主题/theme(指明暗模式,另一项独立设置)、页面缩放/page zoom(此处缩放仅作用于图标本身)。

**图标类型 (Icon Type)**:
类型注册表中的一个条目,声明该类图标:详情容器形态、默认尺寸与支持的尺寸档位、是否单例、配置表单,以及摘要纯函数(ADR-0001 契约;网格渲染已由各类型专属 body 承接,该字段暂无消费方)。类型分基础与扩展两类。
_Avoid_: 组件、widget、block。

**基础类型 / 扩展类型 (Base / Extension Type)**:
基础类型随产品内置、不可卸载(目前仅"网站链接")。扩展类型可插拔(目前为"股票"、"更新日志"),是后续新增各类图标功能的扩展点。

**单例类型 (Singleton Type)**:
注册表中标记为单例的图标类型,全局仅允许一个实例(目前为"更新日志")。新增抽屉中已存在的单例类型会被禁用。非单例类型(网站链接、股票)可添加任意多个实例。

**分组 (Group)**:
一种特殊「图标」:`type='group'` 的图标行(见 ADR-0011),iOS 文件夹式的收纳容器:收起态为玻璃块内成员图标的 3×2 迷你预览(前 6 个),浏览成员走分组弹层(见 ADR-0015)。固定占 1 格、不可切「尺寸」、无嵌套;组名存于图标 `data`,默认「新建分组」,空名回落默认。成员只能是「网站链接」图标(子图标),组内不计「页面容量」;子图标保留自身「尺寸」,移出后按原尺寸落回。建组 / 加入分组 = 编辑模式拖图标悬停到另一图标或分组上合并(同一手势、同一放大反馈);解散 = 编辑模式分组上的 ×,子图标洒回本页;组内排序与移出也仅编辑模式(分组改名除外,走分组弹层点名称)。页面序列与组内序列各自独立按 `sort_order` 排列;组内超过 9 个时弹层按 9 个/页流式分页,分页只是展示切片、不是实体。
_Avoid_: 文件夹、folder(不叫文件夹)、标签组。

**编辑模式 (Edit Mode)**:
右键切换的编排态。在此模式下可拖拽排序、切换尺寸、编辑图标配置(data)、删除图标与管理页面;「分组」的建组、加入、组内排序、移出与解散也仅在此模式(分组改名除外)。再次右键退出。
_Avoid_: 设置模式、customize mode。

**新增抽屉 (Add Drawer)**:
由右上角"+"按钮唤起的侧抽屉,用于选择并配置待新增的图标类型。与编辑模式职责分离——新增图标不在编辑模式内完成。

**公司概述 (Company Overview)**:
股票图标中,描述标的**公司**(而非指数)的相对稳定属性集合——主营业务、所属行业、地区、上市交易所、官网,以及市值/市盈率/市净率等随股价变动的派生量。区别于随行情秒级变动的「实时摘要」(价格/涨跌/成交)与历史价格序列「K线」。指数型标的(如上证指数、纳指)无公司概述,仅有点位/涨跌。
_Avoid_: 基本面、概述信息、公司信息、stock info、overview。

**天气 (Weather)**:
扩展图标类型,非单例——每个实例绑定一个「位置」(城市),展示该位置的当前天气、空气质量与灾害预警。图标 `data` 存该位置的城市名、行政区划(adm1/adm2)与经纬度(lat/lon);经纬度是三套数据(实况/空气/预警)取数的统一坐标,城市搜索(和风 GeoAPI)只在选位置时用一次、解析成经纬度后存下。区别于「自选股」(绑定标的,前端直连行情)与「更新日志」(单例、无位置);天气取数走后端代理(Key 不入前端,见 ADR-0009)。
_Avoid_: 天气磁贴/天气卡片(旧实现叫法)、weather widget。

**重定向扩展 (Redirect Extension)**:
接管浏览器新标签页的 Chrome 扩展,唯一职责是把新标签页导航到「目标页 URL」。它是产品(独立部署的 Web SPA)的**入口基础设施**,不是页面的宿主——不打包任何产品代码,页面的 origin、存储与部署形态均不受它影响。
_Avoid_: 新标签页插件、宿主(见 ADR-0010,扩展不是宿主)。

**目标页 URL (Target Page URL)**:
新标签页最终打开的网页地址,由用户在扩展提供的配置入口修改,出厂默认指向本地开发服务器;本地开发与远程部署只是同一配置的两个取值,扩展不感知环境概念。
_Avoid_: 指定网页、首页(浏览器 homepage 是另一回事)。

### 数据同步与备份

**本地镜像 (Local Mirror)**:
浏览器 IndexedDB 里按用户存的一份完整配置副本(`{pages, icons, layoutSettings}` + `updatedAt` + `dirty`,其中 `layoutSettings` 即「布局设置」),随在线写入实时更新,离线时作临时权威。按 userId 分键,登出不清空(作恢复来源)。主题(theme)为永久深色、不持久化、不入镜像。
_Avoid_: 本地缓存(指 React Query 的内存视图,刷新即失)。

**服务端权威 (Server Authority)**:
在线时服务端是配置的唯一事实来源,写入先到服务端再回写本地镜像;仅服务端不可达时,本地镜像临时接管。
_Avoid_: 主库、master。

**离线待推送 / 脏 (Offline Pending / Dirty)**:
服务端不可达期间只改本地镜像并置 `dirty`,尚未上服务端;重连后经「和解」决定是否推送。
_Avoid_: 未同步、pending changes。

**和解 (Reconciliation)**:
在加载、写入成功后、以及从离线恢复在线时,比较本地"上次服务端确认时间"与服务端 `config_version.updated_at`,新者整份赢(整体-blob LWW)。这是双向恢复的判据。
_Avoid_: 同步(太宽泛)、合并(指导入时的语义合并,另一回事)。

**备份 (Backup) / 恢复 (Recovery)**:
备份 = 导出含 `schemaVersion` 的完整 JSON;恢复 = 从备份或存活的另一端把数据还原到丢失端。导入导出是手动的备份/恢复通道,双端镜像是自动的互保通道,两者目的相同(防丢失)、机制不同。
_Avoid_: 导入导出(是实现动作,非概念)。
