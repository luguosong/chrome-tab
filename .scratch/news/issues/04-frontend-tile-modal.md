# 04 前端接线 + tile + Modal

Status: resolved

types.ts IconTypeId 加 'news';iconTypeRegistry NEWS_DEF(单例 3×2 header 入口);NewsIconBody(BigTile 混合流)+ NewsModal(全部/各源/管理平铺复选);Icon.tsx/DashboardPage.tsx 分发分支;hooks/useNews.ts。注册表纯函数测试。详见 spec.md「定案」UI 节与「测试」节。
